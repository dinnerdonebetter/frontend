{
  "name": "models",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "format": "prettier --write .",
    "format-check": "prettier --check ."
  },
  "devDependencies": {
    "tsconfig": "workspace:*"
  }
}
